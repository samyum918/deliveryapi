spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/delivery?serverTimezone=UTC
    username: root
    password:
google:
  distance-matrix:
    api-key: